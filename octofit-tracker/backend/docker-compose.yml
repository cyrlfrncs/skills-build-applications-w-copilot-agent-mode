version: '3.8'
services:
  mongodb:
    image: mongo:6.0
    container_name: octofit_mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: octofit_db
    volumes:
      - mongo_data:/data/db
volumes:
  mongo_data:
